{
  "name": "seven-roots-botanical-oral-care",
  "version": "1.5.0",
  "private": true,
  "description": "SEVEN ROOTS storefront, payments, Zoho inventory, and Liberia/U.S. staff operations",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "NODE_ENV=development node --watch server/index.js",
    "test": "node --test",
    "check": "node --check server/index.js && node --check server/app.js && node --check server/store.js && node --check server/staff.js && node --check server/payments.js && node --check server/zoho.js && node --check app.js && node --check order-success.js && node --check admin.js && node --check staff.js",
    "security": "node scripts/check-secrets.js"
  },
  "dependencies": {
    "stripe": "22.4.0"
  }
}
